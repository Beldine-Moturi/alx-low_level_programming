#include "main.h"

/**
 * mul - multiply two numbers
 * @a: takes first operand
 * @b: takes send operand
 * Return: return mul on succes
 **/

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
