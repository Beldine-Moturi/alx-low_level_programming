#include "main.h"
/**
 *_abs - computes the absolute value of an int
 *@n: integer whosevalue is computed
 *
 *Return: the absolute value of the int
 *
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);

	return (-n);
}
